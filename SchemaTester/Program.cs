using System; using FirebirdSql.Data.FirebirdClient; class Program { static void Main() { string connStr = "User=SYSDBA;Password=masterkey;Database=C:\\Coliseu\\Data\\PIVETA.FDB;DataSource=localhost;Port=3050;Dialect=3;Charset=NONE;Role=;Connection lifetime=15;Pooling=true;MinPoolSize=0;MaxPoolSize=50;Packet Size=8192;ServerType=0;"; using (var conn = new FbConnection(connStr)) { conn.Open(); using (var cmd = new FbCommand("SELECT RDB$FIELD_NAME FROM RDB$RELATION_FIELDS WHERE RDB$RELATION_NAME = 'PRODUTOS'", conn)) using (var reader = cmd.ExecuteReader()) { Console.WriteLine("PRODUTOS COLUMNS:"); while (reader.Read()) Console.Write(reader.GetString(0).Trim() + ", "); Console.WriteLine(); } } } }
